# Add script to run program here.
# Complete bin/setup so that after it is
# run, bin/parking_lot can be used to launch
# it.
BASEDIR=$(dirname "$0")
java -jar $BASEDIR/../build/libs/parking_lot.jar $*
